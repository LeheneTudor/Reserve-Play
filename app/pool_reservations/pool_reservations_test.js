'use strict';

describe('myApp.pool_reservations module', function() {

  beforeEach(module('myApp.pool_reservations'));

  describe('pool_reservations controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});
'use strict';

describe('reservations.tennis_reservations module', function() {

  beforeEach(module('reservations.tennis_reservations'));

  describe('tennis_reservations controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});